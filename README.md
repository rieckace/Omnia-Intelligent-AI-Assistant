# 🧠 LangChain Chatbot with Memory

A simple academic chatbot project built using **LangChain**, **HuggingFace LLMs**, and **Streamlit**.  
The chatbot remembers past messages using **ConversationBufferMemory**.

## 🚀 Run Locally

1. Clone the repo:
   ```bash
   git clone <repo-url>
   cd langchain-chatbot-memory
